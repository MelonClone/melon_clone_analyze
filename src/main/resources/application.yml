logging:
  level:
    root: info

server:
  address: localhost
  port: 8055

spring:
  jpa:
    show-sql: false
    generate-ddl: true
    # database: mysql
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect

  datasource:
    url: jdbc:mysql://127.0.0.1:3306/melon_clone?serverTimezone=UTC&charaterEncoding=UTF-8
    username: melon_admin
    password: melonclone2020
    driver-class-name: com.mysql.cj.jdbc.Driver
    platform: mysql
    initialization-mode: always
  
  rabbitmq:
    host: 127.0.0.1
    port: 5672
    username: guest
    password: guest